.mx-auto{ class: 'sm:w-1/3' }
  %h1.heading-1.text-center
    = title

  .bg-white.p-8.rounded-lg.border.my-4.shadow-xl
    = form

  .flex.flex-col.text-center.gap-y-1
    - links.each do |link|
      = link
